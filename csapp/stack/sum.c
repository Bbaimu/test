/**
     when the result > 32bit . store the result in %eax in not enough
	 so we should use %edx(high) and %eax (low)
****/


void sum()
{
	int a = 1;
	int b = 2;
	int c = 3;

	long long sum = parameter(1,2,3);
}
